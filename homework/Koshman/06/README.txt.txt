calc_tree receives 1 necessary argument -- the expression and 1 optional argument -- type of the 
expression. By default it's double, options are:
d -- double
i -- int
l -- long

Example:

calc_tree "(1 + 3)/Pi * 4" l