Программа представляет собой реализацию калькулятора на основе метода рекурсивного спуска.
Поддерживаемые операции:
- умножение
- деление
- сложение
- вычитание
- унарный минус

expr = term
    | expr + term
    | expr - term
term = prim
    | term * prim
    | term / prim
prim = number
    | -number
number = [0-9]+

Передача выражения осуществляется через аргумент командной строки, результат выводится в cout.

Компиляция: g++ task2_calc_2.cpp -o calc -O2 --std=c++11
Запуск: ./calc "2 + 3 * 4 - -2"
